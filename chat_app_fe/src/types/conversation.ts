export type Conversation = {
    id: number;
    name: string;
    image: string;
    lastMessage: string;
}

export type MinimalConversation = {
    id: number;
    name: string;
    image: string;
};